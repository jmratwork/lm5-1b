[defaults]
# The CyberRangeCZ dynamic inventory exposes group names with characters Ansible
# considers invalid (e.g. hyphens), which triggers a noisy warning on every run.
# We already reference groups by their exact names, so silence the auto-rename.
force_valid_group_names = ignore
