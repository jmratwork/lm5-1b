---
- name: restart cynet-dc1 services
  ansible.builtin.command:
    cmd: docker compose -f {{ cynet_dc1_compose_file }} restart
  changed_when: true

- name: restart sshd
  ansible.builtin.service:
    name: sshd
    state: restarted
