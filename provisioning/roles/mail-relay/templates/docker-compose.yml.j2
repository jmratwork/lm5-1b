services:
  mailhog:
    image: {{ mail_relay_docker_image }}
    container_name: mailhog
    restart: unless-stopped
    ports:
      - "{{ mail_relay_smtp_port }}:1025"
      - "{{ mail_relay_ui_port }}:8025"

